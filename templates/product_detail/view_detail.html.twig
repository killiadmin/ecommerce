<div class="container">
    <div class="d-flex justify-content-between">
        <a class="btn btn-secondary text-white shadow me-2 fs-5" href="{{ path('app_product') }}" title="Voir la liste des produits">
            <i class="fa-solid fa-arrow-left"></i>
        </a>
        <a class="btn btn-secondary text-white shadow me-2 fs-5" href="#" title="Ajouter un panier">
            <i class="fa-solid fa-cart-shopping"></i>
        </a>
    </div>
    <div class="container border shadow mt-4">
        <div class="text-center my-5">
            <h1>{{ product.title }}</h1>
        </div>
        <div class="text-start">
            <p>{{ product.description }}</p>
        </div>
        <div class="d-flex flex-row-reverse p-3" style="color: grey">
            Créer le {{ product.createdAt | date('d / m / Y') }}
        </div>
    </div>
</div>
